This is a hack to upload opsin.jar (name structure convertor) into Maven repository.

The OPSIN site where all credits should go is http://oscar3-chem.sourceforge.net/

Download opsin-big-0.1.0.jar and copy into dist folder, then run mvn deploy